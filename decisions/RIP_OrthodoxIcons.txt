country_decisions = {

	# The five icons used to live here, because orthodox_specific_window was
	# spent on holy sites. It is not any more: interface/countryreligionview.gui
	# moves the holy-site and fervour panels down, so the icons are back in the
	# panel where vanilla puts them - see common/religions/russian_orthodox.txt.
	# What stays here is the one thing that is not an icon.
	rip_ro_the_first_rome = {
		major = yes
		potential = {
			religion = russian_orthodox
			NOT = { has_country_modifier = rip_ro_the_first_rome }
		}
		provinces_to_highlight = {
			province_id = 118	# Roma
			NOT = {
				AND = {
					owned_by = ROOT
					religion = russian_orthodox
				}
			}
		}
		allow = {
			owns_core_province = 118	# Roma
			118 = {
				religion = russian_orthodox
			}
			prestige = 50
			is_at_war = no
		}
		effect = {
			add_prestige = 25
			add_country_modifier = {
				name = rip_ro_the_first_rome
				duration = -1
			}
			add_patriarch_authority = 0.2
		}
		ai_will_do = {
			factor = 5
		}
	}
}
