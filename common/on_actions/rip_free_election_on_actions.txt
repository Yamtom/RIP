# The free election fires when the throne actually falls vacant, not on a
# timer. The event's own trigger decides who is holding an election - see
# events/FreeElection.txt.

on_monarch_death = {
	events = {
		free_election.1
	}
}
