country_decisions = {
	establish_hajibey_free_port = {
		major = yes
		potential = {
			normal_or_historical_nations = yes
			was_never_end_game_tag_trigger = yes
			NOT = { tag = ODS }
			NOT = { exists = ODS }
			NOT = { tag = PAP }
			NOT = { tag = HLR }
			is_colonial_nation = no
			owns_core_province = 282 # Yedisan / Hajibey
			NOT = { num_of_cities = 11 }
		}
		provinces_to_highlight = {
			province_id = 282
			owned_by = ROOT
			OR = {
				NOT = { development = 20 }
				NOT = { province_has_center_of_trade_of_level = 1 }
				NOT = { has_building = marketplace }
			}
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			has_institution = global_trade
			stability = 1
			adm_power = 100
			dip_power = 150
			282 = {
				is_capital = yes
				development = 20
				province_has_center_of_trade_of_level = 1
				has_building = marketplace
			}
		}
			effect = {
			add_adm_power = -100
			add_dip_power = -150
			change_tag = ODS
			change_government = republic
			add_government_reform = odesa_trade_republic_reform
			set_government_rank = 1
			set_global_flag = odesa_republic_established
			swap_non_generic_missions = yes
			on_change_tag_effect = yes
			if = {
				limit = { has_custom_ideas = no }
				country_event = { id = ideagroups.1 }
			}
			add_republican_tradition = 15
			add_prestige = 15
		}
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 2
				trade_income_percentage = 0.30
			}
			modifier = {
				factor = 0.5
				num_of_cities = 8
			}
		}
	}
}
